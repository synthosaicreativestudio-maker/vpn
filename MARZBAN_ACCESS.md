# 🚀 Доступ к панели Marzban

## ✅ SSH туннель установлен!

Вы видите страницу с 3D анимацией — это нормально. Это загрузочный экран Marzban.

## 📋 Что делать:

### Вариант 1: Подождать и обновить страницу

1. Подождите 10-30 секунд
2. Нажмите `F5` или `Cmd+R` для обновления страницы
3. Должна появиться панель входа или главная страница

### Вариант 2: Попробовать прямой путь

Попробуйте открыть в браузере:
- `http://localhost:8000/dashboard`
- `http://localhost:8000/admin`
- `http://localhost:8000/login`

### Вариант 3: Создать первого администратора

Если панель требует создания администратора, выполните на сервере:

```bash
# В новом терминале (не закрывайте SSH туннель!)
ssh root@37.1.212.51
cd /opt/marzban
docker compose exec marzban marzban cli admin create
```

Следуйте инструкциям для создания администратора.

### Вариант 4: Использовать CLI для добавления конфигурации

Если панель не загружается, можно добавить конфигурацию через CLI:

```bash
# На сервере
ssh root@37.1.212.51

# Откройте конфигурацию
nano /var/lib/marzban/xray_config.json

# Найдите "inbounds": [ ... ] и добавьте JSON из generated_config.json
# Сохраните (Ctrl+O, Enter, Ctrl+X)

# Перезапустите
cd /opt/marzban
docker compose restart
```

## 📝 После доступа к панели:

1. Перейдите в **Core Settings**
2. Найдите раздел `"inbounds": [ ... ]`
3. Вставьте JSON из `generated_config.json`
4. Сохраните изменения
5. Создайте пользователя (VLESS, Flow: vision)
6. Подключитесь через Amnezia VPN

---

**Попробуйте сначала обновить страницу (F5) и подождать немного!**
