# 💾 Нормальное использование памяти при 20 пользователях

**Для сервера с VLESS+Reality (маскировка под Яндекс) и Marzban**

---

## 📊 Компоненты, которые используют память

### 1. **Xray Core (VLESS+Reality)** - основной VPN
- **Что это:** Программа, которая обрабатывает VPN подключения
- **Память на 1 подключение:** ~5-10 МБ
- **Память на 20 подключений:** ~100-200 МБ
- **Базовая память (без подключений):** ~50-100 МБ

### 2. **Marzban (панель управления)** - в Docker
- **Что это:** Веб-панель для управления пользователями
- **Память:** ~200-300 МБ
- **Не зависит от количества пользователей** (только от количества активных подключений)

### 3. **Система (Ubuntu)**
- **Что это:** Операционная система
- **Память:** ~200-300 МБ

### 4. **TinyProxy (для бота)**
- **Что это:** HTTP прокси для бота
- **Память:** ~50-100 МБ (не зависит от VPN пользователей)

### 5. **Другие процессы**
- **Что это:** Системные службы, логи и т.д.
- **Память:** ~100-200 МБ

---

## ✅ НОРМАЛЬНОЕ использование памяти

### При 20 активных пользователях VPN:

| Компонент | Память | Примечание |
|-----------|--------|------------|
| **Xray Core** | 150-250 МБ | ~10 МБ на подключение |
| **Marzban** | 200-300 МБ | Фиксированно |
| **Система Ubuntu** | 200-300 МБ | Фиксированно |
| **TinyProxy** | 50-100 МБ | Для бота |
| **Другие процессы** | 100-200 МБ | Системные службы |
| **ИТОГО** | **700-1,150 МБ** | **~70-80% от 1 ГБ** |

### При 0 активных пользователях (все отключены):

| Компонент | Память |
|-----------|--------|
| **Xray Core** | 50-100 МБ |
| **Marzban** | 200-300 МБ |
| **Система Ubuntu** | 200-300 МБ |
| **TinyProxy** | 50-100 МБ |
| **Другие процессы** | 100-200 МБ |
| **ИТОГО** | **600-1,000 МБ** | **~60-70% от 1 ГБ** |

---

## 📈 График использования памяти

```
Память (МБ)
│
1000│                                    ╱╲
    │                                 ╱    ╲
 800│                              ╱        ╲
    │                           ╱            ╲
 600│                        ╱                  ╲
    │                     ╱                      ╲
 400│                  ╱                          ╲
    │               ╱                              ╲
 200│            ╱                                  ╲
    │         ╱                                      ╲
   0└────────┴────────┴────────┴────────┴────────┴──
     0       5       10       15       20      Пользователи
```

**Вывод:** Память растет линейно с количеством активных подключений.

---

## 🎯 Что считается НОРМАЛЬНЫМ

### ✅ Нормально (все хорошо):
- **Использование памяти: 60-80%** (600-800 МБ из 1 ГБ)
- **Swap не используется** или используется минимально (< 100 МБ)
- **Сервер работает быстро**, нет задержек

### ⚠️ Внимание (нужно следить):
- **Использование памяти: 80-90%** (800-900 МБ из 1 ГБ)
- **Swap используется умеренно** (100-200 МБ)
- **Сервер работает нормально**, но может быть медленнее

### ❌ Проблема (нужно действовать):
- **Использование памяти: > 90%** (> 900 МБ из 1 ГБ)
- **Swap активно используется** (> 200 МБ)
- **Сервер работает медленно**, есть задержки
- **Процессы зависают**

---

## 🔍 Как проверить использование памяти

### Подключитесь к серверу:
```bash
ssh root@37.1.212.51
```

### Проверьте память:
```bash
free -h
```

**Пример вывода:**
```
              total        used        free      shared  buff/cache   available
Mem:          1.0Gi       750Mi       150Mi        20Mi       150Mi       200Mi
Swap:         2.0Gi       50Mi       1.9Gi
```

**Что смотреть:**
- **Mem used:** Сколько используется (750 МБ = 75% - это нормально)
- **Swap used:** Сколько используется Swap (50 МБ = нормально, если > 200 МБ = проблема)

### Детальная проверка по процессам:
```bash
ps aux --sort=-%mem | head -10
```

**Покажет топ-10 процессов по использованию памяти.**

---

## 📊 Реальные цифры для вашего сервера

### Текущая ситуация (до защиты):
- **Память:** 1 ГБ RAM
- **Использование:** 100% (все использовано)
- **Swap:** Активно используется (проблема!)
- **Причина:** 332+ подключения к TinyProxy (не к VPN!)

### После защиты (ожидаемый результат):
- **Память:** 1 ГБ RAM
- **Использование:** 60-80% (600-800 МБ)
- **Swap:** Минимально или не используется
- **Причина:** Только легитимные подключения

### При 20 пользователях VPN:
- **Память:** 1 ГБ RAM
- **Использование:** 70-85% (700-850 МБ)
- **Swap:** Не используется или минимально (< 50 МБ)
- **Статус:** ✅ Нормально, но на пределе

---

## ⚠️ Важные моменты

### 1. Активные vs Всего пользователей
- **20 пользователей** ≠ **20 активных подключений**
- Обычно одновременно подключено **10-30%** от всех пользователей
- Если у вас 20 пользователей, обычно активно подключено **2-6 человек**

### 2. Маскировка под Яндекс не влияет на память
- **Reality маскировка** (taxi.yandex.ru) использует **столько же памяти**, сколько обычный VLESS
- Разница только в **безопасности**, не в ресурсах

### 3. Память растет с активными подключениями
- **0 подключений:** ~600 МБ
- **10 подключений:** ~700 МБ
- **20 подключений:** ~800 МБ
- **50 подключений:** ~1,000 МБ (проблема на сервере с 1 ГБ!)

---

## 💡 Рекомендации

### Для сервера с 1 ГБ RAM:

#### ✅ Можно обслуживать:
- **20-30 активных подключений** одновременно
- **50-100 пользователей** всего (не все подключаются одновременно)

#### ⚠️ На пределе:
- **30-50 активных подключений** (память будет 90%+, Swap начнет использоваться)

#### ❌ Не рекомендуется:
- **> 50 активных подключений** (сервер будет медленным)

### Для коммерческого использования:

#### Рекомендуется увеличить до:
- **2 ГБ RAM** - для 50-100 активных подключений
- **4 ГБ RAM** - для 100-200 активных подключений
- **8 ГБ RAM** - для 200-500 активных подключений

---

## 📋 Чеклист проверки памяти

### Ежедневная проверка:
```bash
# Подключиться к серверу
ssh root@37.1.212.51

# Проверить память
free -h

# Проверить активные подключения VPN
ss -tn | grep :443 | wc -l

# Проверить использование Swap
free -h | grep Swap
```

### Что должно быть:
- ✅ **Mem used < 80%** (меньше 800 МБ из 1 ГБ)
- ✅ **Swap used < 100 МБ** (или не используется)
- ✅ **Активных подключений VPN < 30** (для сервера с 1 ГБ)

---

## 🎯 Итоговые цифры для вашего случая

### При 20 пользователях VPN (маскировка под Яндекс):

| Показатель | Нормально | Внимание | Проблема |
|------------|-----------|----------|----------|
| **Использование памяти** | 60-80% | 80-90% | > 90% |
| **Память в МБ** | 600-800 МБ | 800-900 МБ | > 900 МБ |
| **Использование Swap** | < 50 МБ | 50-200 МБ | > 200 МБ |
| **Активных подключений** | < 20 | 20-30 | > 30 |

### Реальная ситуация:
- **20 пользователей** → обычно **2-6 активных** одновременно
- **Память:** ~650-750 МБ (65-75%) - ✅ **НОРМАЛЬНО**
- **Swap:** Не используется - ✅ **НОРМАЛЬНО**

---

## 📝 Резюме

**Для вашего сервера (1 ГБ RAM) с 20 пользователями VPN:**

✅ **Нормальное использование памяти: 65-75%** (650-750 МБ)  
✅ **Swap не используется** или минимально  
✅ **Сервер работает быстро**  
✅ **Маскировка под Яндекс не влияет на память**

**Если память > 85%** - нужно либо:
1. Увеличить RAM до 2 ГБ
2. Или ограничить количество одновременных подключений

---

**Вопросы?** Спросите, если что-то непонятно!
