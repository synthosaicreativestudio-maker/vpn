# 🔧 Решение проблем с доступом к Marzban

## Проблема: Отображается странная 3D графика вместо панели

Если вы видите 3D wireframe объект на черном фоне вместо панели управления Marzban, попробуйте:

### Решение 1: Обновить страницу
Нажмите `F5` или `Cmd+R` (на Mac) для обновления страницы.

### Решение 2: Очистить кеш браузера
1. Нажмите `Cmd+Shift+Delete` (Mac) или `Ctrl+Shift+Delete` (Windows)
2. Очистите кеш и cookies
3. Перезагрузите страницу

### Решение 3: Попробовать другой браузер
Откройте `http://localhost:8000` в другом браузере (Safari, Firefox, Edge).

### Решение 4: Проверить правильный URL
Убедитесь, что вы открываете:
```
http://localhost:8000
```
А не `https://localhost:8000` (без SSL).

### Решение 5: Проверить, что Marzban полностью запустился
Подождите 30-60 секунд после подключения SSH туннеля, затем обновите страницу.

### Решение 6: Проверить логи Marzban
Если проблема сохраняется, проверьте логи:
```bash
# В новом терминале
ssh root@37.1.212.51
docker logs marzban-marzban-1 --tail 50
```

## Альтернативный доступ

Если панель не загружается, можно добавить конфигурацию напрямую через файлы на сервере:

1. Подключитесь к серверу:
```bash
ssh root@37.1.212.51
```

2. Откройте конфигурацию:
```bash
nano /var/lib/marzban/xray_config.json
```

3. Найдите раздел `"inbounds": [ ... ]` и добавьте JSON из `generated_config.json`

4. Перезапустите Marzban:
```bash
cd /opt/marzban
docker compose restart
```

---

**Попробуйте сначала обновить страницу (F5 или Cmd+R)!**
