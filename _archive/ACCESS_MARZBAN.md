# 🔧 Доступ к панели Marzban

## ⚠️ Проблема с доступом

Marzban работает на порту **8000**, но слушает только на `127.0.0.1` (localhost), поэтому доступ извне невозможен.

## 🔧 Решение 1: SSH туннель (рекомендуется)

Создайте SSH туннель на вашем компьютере:

```bash
ssh -L 8000:127.0.0.1:8000 root@37.1.212.51
```

Затем откройте в браузере:
```
http://localhost:8000
```

## 🔧 Решение 2: Исправить конфигурацию

Если нужно, чтобы Marzban был доступен напрямую по IP:

1. Подключитесь к серверу:
```bash
ssh root@37.1.212.51
```

2. Проверьте конфигурацию:
```bash
cd /opt/marzban
cat .env | grep UVICORN
```

3. Убедитесь, что указано:
```
UVICORN_HOST = "0.0.0.0"
UVICORN_PORT = 8000
```

4. Перезапустите контейнер:
```bash
cd /opt/marzban
docker-compose restart
```

5. Откройте порт в файрволе:
```bash
ufw allow 8000/tcp
```

6. Откройте в браузере:
```
http://37.1.212.51:8000
```

## 📋 После доступа к панели

1. Вставьте JSON из `generated_config.json` в Core Settings → inbounds
2. Создайте пользователя (VLESS, Flow: vision)
3. Подключитесь через Amnezia VPN

---

**Рекомендую использовать SSH туннель для безопасности!**
