# 📊 Как посмотреть использование ресурсов сервера

## 🖥️ Через панель is*hosting

### Вариант 1: Вкладка "Обзор"
В панели управления уже видно:
- **Трафик:** Входящий 236.18 Gb / Исходящий 240.34 Gb из 2 Tb
- **Статус:** Сервер запущен, Uptime: 34 дня

### Вариант 2: Вкладка "Терминал"
1. Откройте вкладку **"Терминал"** в панели is*hosting
2. Выполните команды ниже

---

## 💻 Команды для терминала

### Быстрая проверка всех ресурсов:
```bash
# Скопируйте и выполните в терминале панели is*hosting
./check_resources.sh
```

Или выполните команды по отдельности:

### 1. Использование диска (20GB SSD)
```bash
df -h
```
**Покажет:** Сколько места занято и сколько свободно

### 2. Использование RAM (1GB)
```bash
free -h
```
**Покажет:** Сколько памяти используется и сколько свободно

### 3. Использование CPU
```bash
top
# Нажмите 'q' для выхода
```
Или быстрая проверка:
```bash
uptime
```

### 4. Трафик (уже видно в панели)
В панели is*hosting уже показано:
- **Лимит:** 2 Tb
- **Использовано:** ~476 Gb (236 входящий + 240 исходящий)
- **Осталось:** ~1.5 Tb

### 5. Размер Docker контейнеров
```bash
docker system df
```

### 6. Размер логов
```bash
du -sh /var/log/*
```

---

## 📋 Что проверить в первую очередь

### ⚠️ Критично:
1. **Диск:** `df -h` - должно быть свободно минимум 2-3GB
2. **RAM:** `free -h` - должно быть свободно минимум 200-300MB

### 📊 Полезно знать:
- **CPU:** Обычно не проблема на VPS
- **Трафик:** Видно в панели (осталось ~1.5 Tb)

---

## 🚨 Если ресурсов мало

### Диск переполнен:
```bash
# Очистка Docker
docker system prune -a

# Очистка логов
journalctl --vacuum-time=7d

# Удаление старых логов
find /var/log -type f -name "*.log" -mtime +30 -delete
```

### RAM переполнена:
```bash
# Перезапуск Docker контейнеров
cd /opt/marzban
docker compose restart
```

---

## 📱 Быстрая команда (все сразу)

Скопируйте и выполните в терминале панели:

```bash
echo "=== ДИСК ===" && df -h / && echo "" && \
echo "=== RAM ===" && free -h && echo "" && \
echo "=== CPU ===" && uptime && echo "" && \
echo "=== DOCKER ===" && docker system df 2>/dev/null || echo "Docker не запущен"
```

---

**Совет:** Сохраните скрипт `check_resources.sh` на сервер для регулярной проверки.
