# ✅ Как проверить работу VPN

## 🔍 Проверка 1: Статус сервера

### Проверка порта 443 на сервере:

```bash
ssh root@37.1.212.51
netstat -tlnp | grep 443
# или
ss -tlnp | grep 443
```

Должно быть видно, что порт 443 слушается процессом `xray`.

### Проверка логов Marzban:

```bash
ssh root@37.1.212.51
docker logs marzban-marzban-1 --tail 50
```

Должны быть сообщения о запуске Xray core без ошибок.

---

## 🔍 Проверка 2: Подключение через Amnezia VPN

### Вариант A: Использование ссылки vless://

1. Скопируйте ссылку из файла `vless_connection_link.txt`
2. Откройте Amnezia VPN
3. Нажмите **"Вставить"** в поле "Вставьте ключ"
4. Ссылка должна автоматически распознаться
5. Нажмите **Подключиться**

### Вариант B: Использование файла конфигурации

1. Откройте Amnezia VPN
2. Нажмите **"Файл с настройками подключения"**
3. Выберите файл `amnezia_config.json` или `v2ray_config.json`
4. Подключитесь

---

## 🔍 Проверка 3: Проверка IP адреса

### После подключения к VPN:

1. Откройте браузер
2. Перейдите на один из сайтов:
   - https://ip-api.com
   - https://whatismyipaddress.com
   - https://2ip.ru
   - https://ifconfig.me

3. **Проверьте IP адрес** — он должен быть `37.1.212.51` (IP вашего сервера)

### Если IP не изменился:

- Убедитесь, что VPN подключен (иконка должна быть активна)
- Проверьте, что трафик идет через VPN (в настройках Amnezia)
- Попробуйте переподключиться

---

## 🔍 Проверка 4: Проверка DNS

### Проверка DNS утечек:

1. Откройте: https://dnsleaktest.com
2. Нажмите **"Extended Test"**
3. Проверьте, что DNS серверы не вашего провайдера

---

## 🔍 Проверка 5: Скорость соединения

### Проверка скорости через VPN:

1. Откройте: https://fast.com или https://speedtest.net
2. Запустите тест скорости
3. Сравните со скоростью без VPN

**Ожидаемая скорость:** Может быть немного ниже из-за шифрования, но должна быть приемлемой.

---

## 🔍 Проверка 6: Проверка портов и сервисов

### На вашем компьютере (Mac):

```bash
# Проверка локальных портов VPN
lsof -i | grep -E '1080|10808|10809'
```

Должны быть видны порты, которые использует Amnezia VPN.

---

## 🔍 Проверка 7: Логи подключения

### Проверка логов на сервере:

```bash
ssh root@37.1.212.51
docker logs marzban-marzban-1 -f
```

При подключении клиента вы должны видеть логи подключений.

---

## ⚠️ Решение проблем

### Проблема: Не подключается

**Проверьте:**
1. Порт 443 открыт в файрволе: `ufw status | grep 443`
2. Конфигурация правильная: проверьте файл на сервере
3. Ключи совпадают: Public Key должен быть `n5E8KcFHjef-ZC2mKjzkVldLJiLrsjfpE1Z-XmLfxH4`

### Проблема: Подключается, но IP не меняется

**Проверьте:**
1. Настройки маршрутизации в Amnezia VPN
2. Режим работы VPN (глобальный или по правилам)
3. Попробуйте другой сайт для проверки IP

### Проблема: Медленная скорость

**Возможные причины:**
1. Географическое расстояние до сервера
2. Нагрузка на сервер
3. Ограничения провайдера

---

## 📋 Чек-лист проверки:

- [ ] Порт 443 слушается на сервере
- [ ] Marzban запущен и работает
- [ ] Конфигурация добавлена в Marzban
- [ ] Пользователь создан
- [ ] Файл конфигурации создан
- [ ] Amnezia VPN подключается
- [ ] IP адрес изменился на 37.1.212.51
- [ ] DNS не утекает
- [ ] Скорость приемлемая

---

**После выполнения всех проверок VPN должен работать корректно! 🚀**
