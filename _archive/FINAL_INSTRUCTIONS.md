# 🎯 Финальные инструкции - Автоматическое выполнение

## ✅ Все готово к автоматическому выполнению!

Я создал полностью автоматизированные скрипты, которые выполнят все шаги настройки.

## 🚀 Вариант 1: Полностью автоматический (рекомендуется)

### На вашем компьютере:

```bash
# Перейдите в директорию с файлами
cd /Users/verakoroleva/Desktop/vpn

# Запустите автоматический скрипт
python3 execute_setup.py
```

Скрипт автоматически:
- ✅ Подключится к серверу (37.1.212.51)
- ✅ Скопирует скрипт настройки на сервер
- ✅ Выполнит все команды на сервере
- ✅ Сгенерирует ключи REALITY
- ✅ Создаст JSON конфигурацию
- ✅ Сохранит конфигурацию локально в `generated_config.json`

**Если автоматическое подключение не работает** (нет SSH ключей или sshpass), используйте Вариант 2.

---

## 🚀 Вариант 2: Полуавтоматический (через SSH)

### Шаг 1: Скопируйте скрипт на сервер

```bash
# С вашего компьютера
scp auto_setup_reality_non_interactive.sh root@37.1.212.51:/tmp/
```

Или используйте любой другой способ (SFTP, rsync и т.д.)

### Шаг 2: Подключитесь к серверу и запустите

```bash
# Подключение к серверу
ssh root@37.1.212.51

# На сервере: сделайте скрипт исполняемым и запустите
chmod +x /tmp/auto_setup_reality_non_interactive.sh
/tmp/auto_setup_reality_non_interactive.sh
```

Скрипт автоматически выполнит все шаги и создаст конфигурацию в `/tmp/marzban_reality_config.json`

### Шаг 3: Скопируйте конфигурацию обратно (опционально)

```bash
# С вашего компьютера
scp root@37.1.212.51:/tmp/marzban_reality_config.json ./generated_config.json
```

---

## 📋 Что делать после выполнения скрипта

### 1. Получите JSON конфигурацию

Конфигурация будет в одном из мест:
- Локально: `generated_config.json` (если использовали Python скрипт)
- На сервере: `/tmp/marzban_reality_config.json`

### 2. Вставьте конфигурацию в Marzban

1. Откройте панель Marzban в браузере:
   - Обычно: `http://37.1.212.51:62050` или другой порт
   - Или найдите порт: `docker ps | grep marzban`

2. Перейдите в **Core Settings**

3. Найдите раздел `"inbounds": [ ... ]`

4. **Вставьте JSON конфигурацию** внутрь квадратных скобок

5. Если там уже есть конфиг на порту 443 — **удалите или замените его**

6. **Сохраните изменения**

### 3. Создайте пользователя

1. В Marzban перейдите в **Users** → **Add User**

2. Заполните:
   - **Username**: любое имя (например: `vpn-user-1`)
   - **Protocol**: **VLESS**
   - **Flow**: **vision** (xtls-rprx-vision)
   - **Inbound**: **VLESS-Reality-Microsoft**

3. Сохраните пользователя

4. **Скопируйте ссылку `vless://...`** — она понадобится для подключения

### 4. Подключитесь через Amnezia VPN

#### На Android/iOS:
1. Откройте **Amnezia VPN**
2. Нажмите **"Добавить сервер"** или **"+"**
3. Выберите **"Из буфера обмена"**
4. Вставьте ссылку `vless://...`
5. Нажмите **Подключиться**

#### На Windows/Mac:
1. Откройте **Amnezia VPN**
2. Нажмите **"Добавить подключение"**
3. Выберите **"Импорт из буфера обмена"**
4. Вставьте ссылку `vless://...`
5. Сохраните и подключитесь

---

## ✅ Проверка работы

1. Подключитесь через Amnezia VPN
2. Откройте браузер и перейдите на https://ip-api.com
3. Проверьте, что ваш IP изменился на `37.1.212.51`

---

## 🆘 Если что-то не работает

### Проблема: Не могу подключиться по SSH

**Решение:**
- Убедитесь, что SSH доступен: `ping 37.1.212.51`
- Проверьте, что у вас есть SSH ключи или пароль
- Попробуйте подключиться вручную: `ssh root@37.1.212.51`

### Проблема: Скрипт не находит контейнер Marzban

**Решение:**
```bash
# На сервере проверьте контейнеры
docker ps

# Если контейнер называется по-другому, отредактируйте скрипт
# или выполните команды вручную:
docker exec -it ИМЯ_КОНТЕЙНЕРА xray x25519
```

### Проблема: Порт 443 занят

**Решение:**
```bash
# На сервере проверьте, что занимает порт
sudo lsof -i :443

# Если это старый конфиг Marzban, удалите его из inbounds
# Если другой сервис, остановите его
```

### Проблема: Не подключается через Amnezia

**Проверьте:**
1. Правильность Public Key в ссылке `vless://`
2. Правильность SNI (должен быть `www.microsoft.com`)
3. Правильность Flow (`xtls-rprx-vision`)
4. Открыт ли порт 443 в файрволе:
   ```bash
   sudo ufw allow 443/tcp
   # или
   sudo firewall-cmd --add-port=443/tcp --permanent
   sudo firewall-cmd --reload
   ```

---

## 📝 Созданные файлы

- ✅ `auto_setup_reality.sh` - Основной скрипт (интерактивный)
- ✅ `auto_setup_reality_non_interactive.sh` - Неинтерактивная версия
- ✅ `remote_setup.sh` - Скрипт для удаленного выполнения
- ✅ `execute_setup.py` - Python скрипт для полной автоматизации
- ✅ `marzban_reality_config.json` - Шаблон конфигурации
- ✅ `generated_config.json` - Сгенерированная конфигурация (после выполнения)

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ✅ TinyProxy на порту 8080 (работает как раньше)
- ✅ VLESS + REALITY на порту 443 (новый VPN)
- ✅ Готовая конфигурация для Marzban
- ✅ Ссылка для подключения через Amnezia VPN

**Успешной настройки! 🚀**
