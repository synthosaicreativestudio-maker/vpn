# 🔍 Отчет о диагностике VPN сервера

**Дата:** $(date)
**Сервер:** 37.1.212.51

## ❌ Обнаруженные проблемы

### 1. SSH подключение не работает
**Ошибка:** `Permission denied (publickey,password)`

**Возможные причины:**
- Пароль SSH был изменен
- Аутентификация по паролю отключена на сервере
- Изменены настройки SSH (только ключи)
- Сервер заблокировал IP адрес из-за множественных попыток подключения

### 2. Не удалось проверить состояние сервисов
Из-за проблем с SSH не удалось проверить:
- Статус Docker контейнеров
- Статус порта 443
- Логи Marzban
- Конфигурацию Xray
- Статус файрвола

---

## 🔧 Решения

### Вариант 1: Проверка пароля SSH
Если у вас есть доступ к серверу через другой метод (VPS панель, консоль провайдера):

```bash
# Попробуйте подключиться вручную
ssh root@37.1.212.51
# Введите пароль: LEJ6U5chSK
```

Если пароль не подходит, возможно он был изменен.

### Вариант 2: Использование SSH ключей
Если на сервере настроена аутентификация по ключам:

```bash
# Используйте ваш приватный ключ
ssh -i ~/.ssh/your_private_key root@37.1.212.51
```

### Вариант 3: Доступ через VPS панель провайдера
Если у вас есть доступ к панели управления VPS (например, DigitalOcean, Vultr, Hetzner):

1. Войдите в панель управления
2. Откройте консоль сервера (Console/VNC)
3. Выполните команды диагностики напрямую

### Вариант 4: Проверка через веб-интерфейс (если доступен)
Если Marzban UI был доступен ранее через SSH туннель, попробуйте:

1. Проверить, открыт ли порт 8000 наружу (если был настроен)
2. Попробовать подключиться напрямую: `http://37.1.212.51:8000`

---

## 📋 Команды для диагностики (выполнить на сервере)

После получения доступа к серверу выполните:

```bash
# 1. Проверка Docker контейнеров
docker ps | grep marzban

# 2. Проверка порта 443
netstat -tlnp | grep 443
# или
ss -tlnp | grep 443

# 3. Логи Marzban
docker logs marzban-marzban-1 --tail 50

# 4. Статус Docker
systemctl status docker

# 5. Статус файрвола
ufw status

# 6. Проверка конфигурации
cat /var/lib/marzban/xray_config.json | python3 -m json.tool | grep -A 20 "VLESS-Reality"

# 7. Все Docker контейнеры (включая остановленные)
docker ps -a
```

---

## 🚨 Возможные причины неработающего VPN

### Если Docker контейнер остановлен:
```bash
cd /opt/marzban
docker compose restart
```

### Если порт 443 закрыт в файрволе:
```bash
ufw allow 443/tcp
ufw reload
```

### Если Docker не запущен:
```bash
systemctl start docker
systemctl enable docker
```

### Если конфигурация повреждена:
Проверьте файл `/var/lib/marzban/xray_config.json` на наличие синтаксических ошибок:
```bash
cat /var/lib/marzban/xray_config.json | python3 -m json.tool
```

Если есть ошибки, восстановите конфигурацию из бэкапа или пересоздайте inbound в Marzban UI.

---

## 📞 Следующие шаги

1. **Получите доступ к серверу** (через VPS панель или исправив SSH)
2. **Выполните команды диагностики** из раздела выше
3. **Определите проблему** по результатам диагностики
4. **Примените соответствующее решение** из списка выше

---

## 💡 Быстрая проверка доступности сервера

Если у вас есть доступ к другому серверу или компьютеру:

```bash
# Проверка доступности порта 443
nc -zv 37.1.212.51 443

# Проверка доступности порта 22 (SSH)
nc -zv 37.1.212.51 22

# Проверка доступности порта 8080 (TinyProxy)
nc -zv 37.1.212.51 8080
```

Если порт 443 открыт, но VPN не работает - проблема в конфигурации или сервисе.
Если порт 443 закрыт - проблема в файрволе или сервисе не запущен.

---

**Важно:** Сохраните этот отчет и результаты диагностики для дальнейшего решения проблемы.
