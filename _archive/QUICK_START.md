# ⚡ Быстрый старт: VLESS + REALITY для Marzban

## 🚀 Автоматическая настройка (рекомендуется)

### Вариант 1: Полностью автоматический скрипт

```bash
# На вашем VPS сервере
./auto_setup_reality.sh
```

Скрипт автоматически выполнит все шаги и создаст готовую конфигурацию!

---

## 📝 Ручная настройка (три простых шага)

### 1️⃣ Генерация ключей

```bash
docker exec -it marzban xray x25519
```

**Или используйте скрипт:**
```bash
./generate_keys.sh
```

Сохраните **Private Key** и **Public Key**!

---

### 2️⃣ Вставка конфигурации

1. Откройте файл `marzban_reality_config.json`
2. Замените `ЗАМЕНИТЕ_НА_PRIVATE_KEY_ИЗ_КОМАНДЫ_xray_x25519` на ваш **Private Key**
3. Замените `ЗАМЕНИТЕ_НА_PUBLIC_KEY_ИЗ_КОМАНДЫ_xray_x25519` на ваш **Public Key**
4. Зайдите в **Marzban → Core Settings → inbounds**
5. Вставьте JSON в массив `inbounds: [ ... ]`
6. Сохраните изменения

**⚠️ Если на порту 443 уже есть конфиг — удалите его или замените!**

---

### 3️⃣ Создание пользователя и подключение

1. **Marzban → Users → Add User**
2. Выберите:
   - Protocol: **VLESS**
   - Flow: **vision** (xtls-rprx-vision)
   - Inbound: **VLESS-Reality-Microsoft**
3. Скопируйте ссылку `vless://...`
4. Откройте **Amnezia VPN → Добавить сервер → Из буфера обмена**
5. Вставьте ссылку и подключитесь

---

## 📋 Проверка

```bash
# Проверка порта 443
sudo netstat -tlnp | grep 443

# Проверка логов Marzban
docker logs marzban -f
```

---

## 🔧 Если что-то не работает

1. **Порт занят?** Проверьте: `sudo lsof -i :443`
2. **Не подключается?** Проверьте файрвол: `sudo ufw allow 443/tcp`
3. **Ошибки в логах?** Проверьте правильность ключей в конфиге

---

**Подробная инструкция:** См. `MARZBAN_VLESS_REALITY.md`
