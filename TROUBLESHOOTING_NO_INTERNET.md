# 🔧 Решение проблемы: Нет интернета через VPN

## ⚠️ Проблема: VPN подключен, но интернета нет

### Быстрые решения:

## 1️⃣ Используйте исправленный файл конфигурации

Я создал исправленный файл: **`amnezia_config_fixed.json`**

Этот файл содержит:
- ✅ Правильную маршрутизацию трафика
- ✅ Настройки DNS (8.8.8.8, 1.1.1.1)
- ✅ Правильные порты для локальных прокси
- ✅ Исключения для локальных адресов

**Действия:**
1. Отключите VPN в Amnezia
2. Удалите старую конфигурацию
3. Загрузите файл `amnezia_config_fixed.json`
4. Подключитесь снова

## 2️⃣ Проверьте настройки Amnezia VPN

### В настройках Amnezia VPN:

1. Откройте **Настройки** (иконка шестеренки)
2. Проверьте:
   - **Режим работы**: Должен быть "Глобальный" или "По правилам"
   - **DNS**: Должен быть автоматический или 8.8.8.8
   - **Маршрутизация**: Все трафик через VPN

## 3️⃣ Используйте ссылку вместо файла

Попробуйте использовать ссылку `vless://` из файла `vless_connection_link.txt`:

1. Откройте Amnezia VPN
2. Нажмите **"Вставить"** в поле "Вставьте ключ"
3. Вставьте ссылку из `vless_connection_link.txt`
4. Подключитесь

## 4️⃣ Проверьте сервер

### Проверка на сервере:

```bash
ssh root@37.1.212.51
# Пароль: LEJ6U5chSK

# Проверка порта 443
netstat -tlnp | grep 443

# Проверка логов
docker logs marzban-marzban-1 --tail 50

# Проверка интернета на сервере
curl -I https://www.google.com
```

## 5️⃣ Проверьте файрвол

### На вашем Mac:

```bash
# Проверьте, не блокирует ли файрвол
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
```

### На сервере:

```bash
ssh root@37.1.212.51
ufw status
# Порт 443 должен быть открыт
```

## 6️⃣ Альтернативные DNS

Если проблема с DNS, попробуйте вручную указать DNS:

1. Системные настройки Mac → Сеть
2. Выберите активное подключение
3. Дополнительно → DNS
4. Добавьте: `8.8.8.8`, `8.8.4.4`, `1.1.1.1`

## 7️⃣ Проверка подключения

### Проверьте, что VPN действительно подключен:

```bash
# На Mac в терминале
curl -s https://ip-api.com/json | grep -E 'query|country'
```

Если IP не изменился на `37.1.212.51`, VPN не работает.

## 8️⃣ Перезапуск Amnezia VPN

1. Полностью закройте Amnezia VPN
2. Откройте снова
3. Загрузите конфигурацию заново
4. Подключитесь

## 9️⃣ Проверка логов Amnezia

Если в Amnezia VPN есть возможность просмотра логов:
- Откройте логи и проверьте на ошибки
- Ищите сообщения о подключении к серверу

## 🔟 Тест с другим клиентом

Попробуйте использовать другой клиент для проверки:
- v2rayNG (Android)
- V2RayU (Mac)
- Qv2ray (кроссплатформенный)

---

## 📋 Чек-лист диагностики:

- [ ] Использован исправленный файл `amnezia_config_fixed.json`
- [ ] Настройки Amnezia VPN правильные
- [ ] Порт 443 открыт на сервере
- [ ] Сервер имеет доступ в интернет
- [ ] DNS настроены правильно
- [ ] Файрвол не блокирует
- [ ] VPN действительно подключен (IP изменился)
- [ ] Amnezia VPN перезапущен

---

## 🆘 Если ничего не помогает:

1. **Проверьте конфигурацию на сервере:**
```bash
ssh root@37.1.212.51
cat /var/lib/marzban/xray_config.json | python3 -m json.tool | grep -A 20 "VLESS-Reality"
```

2. **Проверьте логи в реальном времени:**
```bash
ssh root@37.1.212.51
docker logs marzban-marzban-1 -f
```
Затем попробуйте подключиться и смотрите логи.

3. **Попробуйте создать нового пользователя** через веб-панель Marzban

---

**Начните с использования файла `amnezia_config_fixed.json` - он должен решить проблему!**
