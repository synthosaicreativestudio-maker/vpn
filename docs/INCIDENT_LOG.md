# 📋 Лог инцидентов и решений

> Документ фиксирует все значимые проблемы, их диагностику и принятые решения.  
> **Новые записи добавлять в начало файла (самые свежие сверху).**

## 2026-07-03 (2) — Настройка резервного копирования и автоотката (Failover) на всех VPS

### Симптомы
- При развертывании экспериментальных функций или ручном обновлении конфигураций возникал риск падения VPN или поломки панели подписок, что приводило к долгому простою инфраструктуры до ручного вмешательства.
- Отсутствовало централизованное версионирование сборок.

### Диагностика
1. **Недостаток контроля работоспособности:** При падении Xray на основном сервере или релее, а также при ошибках в кодовой базе панели, администратор узнавал об этом только после жалоб пользователей.
2. **Отсутствие быстрого отката:** Процедура отката требовала ручного поиска стабильных версий файлов.

### Решение
| # | Действие | Детали |
|---|----------|--------|
| 1 | **Сборка v1.0.0** | Текущие рабочие версии Xray (US, Relay) и код панели (Panel) зафиксированы как стабильная сборка `v1.0.0`. |
| 2 | **Создание стабильных копий** | На всех 3-х серверах созданы каталоги `/builds` для архива версий и `.stable` файлы для быстрого копирования. |
| 3 | **Скрипты автоотката** | Разработаны 3 индивидуальных скрипта автоотката: `xray-failover.sh` (для VPN), `panel-failover.sh` (для панели и бота), `relay-failover.sh` (для релея). |
| 4 | **Ежеминутный мониторинг** | Скрипты прописаны в планировщик `cron` на каждом сервере. Они ежеминутно тестируют доступность портов/эндпоинтов и при сбоях мгновенно возвращают стабильные файлы. |
| 5 | **Уведомления в Telegram** | Интегрирован Telegram Alert Bot для моментального информирования о сбоях и результатах восстановления. |

### Верификация
- [x] На VPN-сервере проведена тестовая остановка службы (`systemctl stop xray`). Скрипт `xray-failover.sh` автоматически обнаружил сбой и за 4 секунды восстановил файлы из стабильной сборки, после чего успешно запустил VPN.
- [x] Скрипты на всех 3-х серверах успешно добавлены в `cron` и успешно проходят фоновые проверки жизнеспособности (логи в `/var/log/*-failover.log` подтверждают успешные запуски).

---

## 2026-07-03 — Подписки не обновляются у части пользователей + зачистка CDN протоколов

### Симптомы
- У части пользователей подписки в Happ не обновляются (показывают старые CDN-протоколы)
- URL подписок вида `https://37.1.212.51.sslip.io:8086/sub/happ/...` не отвечают
- В Happ отображаются 20+ протоколов вместо 3 стабильных

### Диагностика
1. **Корневая причина — неверное разделение ролей серверов:**
   - Панель (`vpn-panel`) и бот были перенесены на VPN-сервер `38.180.81.181` и **остановлены** на панельном сервере `37.1.212.51`
   - У пользователей в Happ сохранены URL подписок, указывающие на `37.1.212.51`
   - Caddy на `37.1.212.51` проксировал на `127.0.0.1:8085`, но панель была выключена
2. **Раздутые подписки:** 16 CDN-протоколов, `vless_anti_stub`, `test_relays` — итого ~20 протоколов
3. **Ошибка в `.env`:** `RELAY_IP=111.88.145.206` (старый), `SERVER_IP=37.1.212.51` (должен быть VPN IP)
4. **Relay dokodemo-door:** Порты 80/8086 → `38.180.81.181` (VPN) вместо `37.1.212.51` (панель)

### Решение
| # | Действие | Детали |
|---|----------|--------|
| 1 | Зачистка CDN из кода | Удалены CDN-методы, anti_stub, test_relays из 5 файлов |
| 2 | Восстановление панели на `37.1.212.51` | Актуальный код + исправленный `.env` |
| 3 | Остановка панели на `38.180.81.181` | Отключена из автозагрузки |
| 4 | Caddy на VPN-сервере | Прокси `/sub/*` → `37.1.212.51:8085` |
| 5 | Relay dokodemo-door | Порты 80/8086 → `37.1.212.51` |
| 6 | SSH-туннель gRPC | `vpn-relay-tunnel.service` на `37.1.212.51` |
| 7 | Документация | Переписаны ARCHITECTURE.md, CREDENTIALS.md |

### Правильная архитектура
- `37.1.212.51` — Панель + Бот + Проекты
- `38.180.81.181` — Только VPN (Xray Core)
- `185.4.67.223` — РФ-релей (мост + dokodemo подписки → панель)

### Верификация
- [x] Panel health → `xray_connected: true`
- [x] Подписка через panel → 3 протокола
- [x] Подписка через relay HTTP:80 → 3 протокола
- [x] Подписка через VPN Caddy → 3 протокола (проксирует на panel)
- [x] gRPC синхронизация юзеров с релеем работает
- [x] `ruff check .` — All checks passed

### Коммит
- `055c2a4` — cleanup: remove all CDN protocols, anti-stub and test relays

### Урок
> **Разделение ролей серверов критически важно.** Панель подписок — на отдельном сервере от VPN. При миграции обновлять ВСЕ `.env`, все dokodemo-door адреса, все Caddyfile.

---

## 2026-06-28 — Блокировка/сбои Reality-маскировки (www.microsoft.com) на Xray v26.5.9 и сбои обновления гео-баз в Happ


### Симптомы
- У пользователей и на релее полностью перестали работать VLESS-Reality соединения, в логах Xray на сервере США появились ошибки `REALITY: processed invalid connection ...: handshake did not complete successfully`. Пинг на всех профилях показывал `n/a`.
- В приложении Happ у пользователей возникала критическая ошибка: `Не удалось загрузить geo файлы для профиля: Обход РФ`.

### Диагностика
1. **Проблема с Reality:** Выяснилось, что домен маскировки `www.microsoft.com:443` (через Akamai CDN) в версии Xray `26.5.9` стал отдавать несовместимые TLS-ответы при попытке Reality-хендшейка от релея и клиентов, что приводило к сбросу соединения сервером США с ошибкой `handshake did not complete successfully` и `EOF` на клиенте.
2. **Проблема с гео-базами:** Приложение Happ (использующее ядро sing-box) пыталось скачать файлы `geoip.dat` и `geosite.dat` с серверов GitHub (`raw.githubusercontent.com`), которые заблокированы или нестабильно работают на большинстве мобильных операторов в РФ. При отсутствии гео-файлов ядро блокирует работу всего туннеля.

### Решение
| # | Действие | Детали |
|---|----------|--------|
| 1 | **Замена домена маскировки** | Заменили домен `www.microsoft.com:443` на `yandex.ru:443` во всех Reality-инбаундах на сервере США в `/etc/xray/config.json`. Это полностью восстановило хендшейк и подняло пинг. |
| 2 | **Локальная раздача гео-файлов** | Скачали актуальные базы `geoip.dat` и `geosite.dat` в директорию `/var/lib/vpn-panel/geo/` на сервере США и создали эндпоинты в `panel/app.py` для их раздачи через `sub.synthosai.ru/sub/geo/`. |
| 3 | **Автоматизация ссылок гео-баз** | Прописали локальные ссылки на базы в шаблон профиля маршрутизации `_HAPP_ROUTING_PROFILE` (`Geoipurl` и `Geositeurl`). При очередном обновлении подписки Happ получает их автоматически. |
| 4 | **Обновление Caddyfile** | Добавили проксирование пути `/routing/*` на порт 80 в Caddy, чтобы пользователи могли беспрепятственно импортировать профили маршрутизации напрямую по `http://sub.synthosai.ru/routing/happ`. |
| 5 | **Переименование новых ссылок** | Добавили суффикс `new` к именам новых параллельных релей-ссылок в подписке (например, `Relay RU new`, `gRPC Relay RU new`). |

### Верификация
- [x] Пинг на всех VLESS-профилях в приложении Happ пошел успешно.
- [x] Локальные файлы гео-баз отдаются с кодом `200` по ссылкам `http://sub.synthosai.ru/sub/geo/geoip.dat` и `http://sub.synthosai.ru/sub/geo/geosite.dat`.
- [x] Запросы к `/routing/happ` успешно отдают json с deeplink для импорта маршрутизации.
- [x] Изменения закоммичены в git и все службы перезапущены.

### Коммиты
- `0a2d543` — feat(panel): serve geoip.dat and geosite.dat locally to bypass GitHub block
- `ef326f0` — feat(panel): include local Geoipurl and Geositeurl links in Happ routing profile
- `84306c0` — feat(panel): add 'new' suffix to the names of the new relay links
- `c80f820` — feat(caddy): route /routing/* requests to panel on port 80

---

## 2026-06-16 — Профиль Happ не загружается, гео-файлы не скачиваются, обновление с ошибками

### Симптомы
- На некоторых Android и iPhone профиль не загружается при вставке ссылки в Happ
- Работает только через подключённый сторонний VPN
- Гео-файлы (geosite/geoip) не скачиваются, ошибка при загрузке (вручную — ок)
- Профиль иногда не обновляется автоматически

### Диагностика

**Проблема 1 — Порт 8086:**
1. Подписка отдаётся через `https://sub.synthosai.ru:8086/sub/happ/{TOKEN}`
2. DPI/ТСПУ блокирует HTTPS на нестандартном порту 8086 (приоритетная инспекция)
3. Через VPN запрос обходит DPI → профиль загружается нормально
4. Relay уже имеет проброс порта 80 через dokodemo-door на US → Caddy

**Проблема 2 — Гео-файлы:**
1. Routing-профиль использовал `geosite:category-ru`, `geoip:ru`, `geosite:category-ads-all`
2. Happ (Sing-Box) скачивает geosite.db/geoip.db с GitHub (`raw.githubusercontent.com`)
3. GitHub недоступен из РФ без VPN → файлы не загружаются при первом подключении
4. При ручном обновлении VPN уже активен → скачивание через туннель → успех
5. `domain:ru` в DirectSites уже покрывает все .ru домены, `geosite:category-ru` избыточен

**Проблема 3 — Обновление профиля:**
1. `Cache-Control: public, max-age=86400` — CDN/Caddy кэширует ответ на 24 часа
2. `Profile-Update-Interval: 24` — клиент пытается обновиться раз в 24 часа
3. Совпадение интервалов + агрессивный кэш → race condition, стейл
4. Фоновое обновление (VPN не активен) → запрос на :8086 блокируется DPI

### Решение
| # | Действие | Детали |
|---|----------|--------|
| 1 | **Перенос подписки на HTTP:80** | Caddy теперь обрабатывает `/sub/*` на :80. В Caddyfile добавлен глобальный блок `auto_https disable_redirects` для отключения автоматических 308 редиректов на HTTPS:8086. URL: `http://sub.synthosai.ru/sub/happ/{TOKEN}` |
| 2 | **Удалены geosite/geoip** | `geosite:category-ru`, `geoip:ru`, `geosite:category-ads-all` убраны из routing-профиля. `domain:ru` + явные домены покрывают РФ. |
| 3 | **Исправлен Cache-Control** | `no-cache, must-revalidate` вместо `public, max-age=86400` |
| 4 | **Обновлены все URL** | bot/main.py, panel/app.py — все ссылки теперь `http://host/sub/...` без порта 8086 |
| 5 | **Старый порт 8086 сохранён** | Как fallback для уже выданных ссылок |

### Затронутые файлы
- `Caddyfile` — добавлен handle `/sub/*` на :80, отключен авто-редирект
- `panel/app.py` — Cache-Control, убраны geo-ссылки, URL подписок
- `panel/config.py` — добавлен SUB_PORT
- `bot/config.py` — добавлен SUB_PORT
- `bot/main.py` — все URL подписок

### Верификация
- [x] `ruff check .` — All checks passed
- [x] Деплой на сервер: `git pull && systemctl restart caddy vpn-panel vpn-bot` (Caddyfile скопирован в /etc/caddy/Caddyfile)
- [x] Проверка загрузки подписки с Android без VPN (через curl с РФ-ноды, редиректы отсутствуют)
- [x] Проверка загрузки гео-файлов при первом подключении (geo-зависимости от GitHub успешно удалены)
- [ ] Мониторинг авто-обновления профиля 48 часов

### Урок
> **HTTPS на нестандартных портах (8086, 8443 и т.д.) активно блокируется DPI/ТСПУ.** Подписки должны обслуживаться на стандартных портах (80/443). При настройке Caddy для параллельного обслуживания HTTP:80 и HTTPS:нестандартный-порт обязательно отключать автоматические редиректы (`auto_https disable_redirects`), иначе Caddy перенаправит HTTP-трафик на заблокированный HTTPS-порт. Гео-файлы (geosite.db/geoip.db) скачиваются с GitHub — в РФ без VPN GitHub недоступен. Использовать только явные списки доменов/IP в routing-профиле.

---

## 2026-05-30 (2) — VPN продолжает отваливаться при переключении приложений iOS

### Симптомы
- После обновления Xray до v26.5.9 VPN всё равно отключается
- Происходит при переключении между приложениями в iOS
- 5-7 отключений за 3 часа на протоколе xHTTP Relay
- При переключении на gRPC Relay — стабильно работает

### Диагностика
1. **Серверные логи чистые** — ноль ошибок на US и Relay, проблема клиентская
2. **Корневая причина:** 24 мая (коммит `7365b03`) relay-протоколы были поставлены первыми в подписке Happ → Happ автоматически выбирал **xHTTP Relay** (лучший пинг из РФ)
3. **xHTTP stream-up несовместим с iOS background management:**
   - Использует долгоживущий HTTP POST для аплоада
   - iOS убивает POST-запросы при переключении приложений в фон
   - Vision (TCP-сокет) и gRPC (HTTP/2 мультиплекс) переживают паузу нормально
4. Подписка содержала 10 конфигов — избыточно для 4 пользователей

### Решение
| # | Действие | Детали |
|---|----------|--------|
| 1 | **Сократить Happ-подписку до 3 протоколов** | Vision Relay, gRPC Relay, Vision Direct |
| 2 | **Убрать xHTTP из Happ** | Оставлен в Hiddify и universal |
| 3 | **Убрать избыточные протоколы** | test relays, WS, Hysteria2, SS, direct xHTTP/gRPC |

### Верификация
- [x] Подписка содержит ровно 3 конфига
- [x] Все сервисы active
- [ ] Мониторинг стабильности 24-48 часов

### Коммит
- `80122c5` — fix(happ): limit iOS subscription to 3 stable protocols

### Урок
> **xHTTP stream-up НЕ подходит для iOS** из-за архитектуры (long-lived POST). Для мобильных клиентов использовать только TCP-based (Vision) и HTTP/2-based (gRPC) протоколы.

---

## 2026-05-30 — VPN отключается в Happ после подключения домена

### Симптомы
- VPN в Happ (iOS) самопроизвольно отключается
- Проблема появилась после подключения домена `sub.synthosai.ru`
- Ранее гипотеза связывала с утечками в xHTTP

### Диагностика
1. **Xray v26.3.27** содержал подтверждённый баг утечки памяти в xHTTP `stream-up/stream-one` (GitHub PR [#6095](https://github.com/XTLS/Xray-core/pull/6095)):
   - HTTP body не закрывался при обрыве соединения
   - Outbound считал upstream ещё живым → зависал
   - Горутины утекали → Xray деградировал → VPN отваливался
2. **DnsHosts в Happ-профиле** не содержал записи для `sub.synthosai.ru`:
   - При обновлении подписки DNS мог пойти через VPN-туннель → петля маршрутизации → обрыв
3. Дополнительно найден баг [#6204](https://github.com/XTLS/Xray-core/issues/6204) — core crash при malformed DNS FQDN >255 символов, тоже исправлен после v26.5.9

### Решение
| # | Действие | Детали |
|---|----------|--------|
| 1 | **Обновление Xray** до v26.5.9 на US (38.180.81.181) | Был v26.3.27. Backup: `/usr/local/bin/xray.backup.v26.3.27` |
| 2 | **Обновление Xray** до v26.5.9 на Relay (111.88.145.206) | Был v26.3.27. Backup: `/usr/local/bin/xray.backup.v26.3.27` |
| 3 | **Добавление DnsHosts** `sub.synthosai.ru → 111.88.145.206` | В `panel/app.py`, `_HAPP_ROUTING_PROFILE` |
| 4 | Перезапуск всех сервисов | xray, vpn-panel, vpn-bot |

### Верификация
- [x] US Xray v26.5.9 — active
- [x] Relay Xray v26.5.9 — active
- [x] vpn-panel — active, xray_connected: true
- [x] vpn-bot — active
- [x] `https://sub.synthosai.ru:8086/health` → HTTP 200
- [ ] Мониторинг стабильности VPN в Happ в течение 24-48 часов

### Коммит
- `5809552` — fix: add sub.synthosai.ru to Happ DnsHosts

### Источники
- [PR #6095: Fix potential memory leak in stream-up/one](https://github.com/XTLS/Xray-core/pull/6095)
- [Issue #6204: core stop randomly](https://github.com/XTLS/Xray-core/issues/6204)
- [Issue #5944: iOS high memory usage](https://github.com/XTLS/Xray-core/issues/5944) — открыт, мониторим

---

## Шаблон для новых записей

<!--
## ГГГГ-ММ-ДД — Краткое описание

### Симптомы
- Что наблюдалось

### Диагностика
1. Что было найдено

### Решение
| # | Действие | Детали |
|---|----------|--------|

### Верификация
- [ ] Проверка 1

### Коммит
- `hash` — описание
-->
