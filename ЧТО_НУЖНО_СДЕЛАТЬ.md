# 📋 Что нужно сделать: Простые шаги для защиты сервера

**Для кого:** Для человека без технических знаний  
**Время выполнения:** 30-60 минут  
**Сложность:** Простая (нужно только копировать команды)

---

## 🎯 Главная проблема

Ваш сервер используется как бесплатный прокси-сервер для всего интернета. Из-за этого:
- Сервер перегружен (332+ подключений)
- Не хватает памяти
- Все работает медленно

**Решение:** Закрыть доступ к прокси для всех, кроме вас.

---

## ✅ ЧТО НУЖНО СДЕЛАТЬ (по порядку)

### ШАГ 1: Узнать свой IP адрес

**Что это:** IP адрес вашего компьютера в интернете (как почтовый адрес, но для интернета)

**Как сделать:**
1. Откройте браузер на вашем Mac
2. Перейдите на сайт: https://ifconfig.me
3. Скопируйте цифры, которые увидите (например: `123.45.67.89`)
4. Запишите их - они понадобятся в следующем шаге

---

### ШАГ 2: Подключиться к серверу

**Что это:** Войти на сервер, чтобы изменить настройки

**Как сделать:**
1. Откройте программу "Терминал" на Mac (найдите через Spotlight: Cmd+Space, введите "Терминал")
2. Скопируйте и вставьте эту команду (нажмите Enter):
   ```bash
   ssh root@37.1.212.51
   ```
3. Когда попросит пароль, введите: `LEJ6U5chSK` (пароль не будет виден - это нормально)
4. Нажмите Enter

**Если все правильно:** Вы увидите строку с `root@...` - это значит, вы на сервере.

---

### ШАГ 3: Ограничить доступ к прокси (только ваш IP)

**Что это:** Разрешить использовать прокси только вам, заблокировать всех остальных

**Как сделать:**
1. Вы все еще должны быть подключены к серверу (если нет - выполните ШАГ 2)
2. Замените `ВАШ_IP` на IP адрес из ШАГА 1 (например, если IP был `123.45.67.89`, то команда будет: `ufw allow from 123.45.67.89...`)
3. Скопируйте и вставьте эту команду (замените `ВАШ_IP`):
   ```bash
   ufw allow from ВАШ_IP to any port 8080 proto tcp comment "TinyProxy access"
   ```
4. Нажмите Enter
5. Теперь заблокируем всех остальных:
   ```bash
   ufw deny 8080/tcp
   ```
6. Нажмите Enter
7. Применим правила:
   ```bash
   ufw reload
   ```
8. Нажмите Enter

**Результат:** Теперь только вы можете использовать прокси на порту 8080.

---

### ШАГ 4: Ограничить количество подключений

**Что это:** Не давать прокси принимать больше 20 подключений одновременно

**Как сделать:**
1. Вы все еще должны быть подключены к серверу
2. Откройте файл с настройками прокси:
   ```bash
   nano /etc/tinyproxy/tinyproxy.conf
   ```
3. Нажмите Enter
4. Найдите строку `#MaxClients 100` (или похожую)
5. Нажмите стрелку вниз, чтобы переместиться к этой строке
6. Удалите символ `#` в начале строки (если он есть)
7. Измените число на `20`, чтобы получилось: `MaxClients 20`
8. Нажмите `Ctrl + O` (сохранить), затем Enter
9. Нажмите `Ctrl + X` (выйти)
10. Перезапустите прокси:
    ```bash
    systemctl restart tinyproxy
    ```
11. Нажмите Enter

**Результат:** Прокси не будет принимать больше 20 подключений одновременно.

---

### ШАГ 5: Установить защиту от атак (fail2ban)

**Что это:** Программа, которая автоматически блокирует подозрительные IP адреса

**Как сделать:**
1. Вы все еще должны быть подключены к серверу
2. Установите программу (скопируйте все команды по очереди):
   ```bash
   apt-get update
   ```
   Нажмите Enter, подождите завершения
   
   ```bash
   apt-get install fail2ban -y
   ```
   Нажмите Enter, подождите завершения (может спросить подтверждение - введите `Y`)

3. Создайте файл с настройками для прокси:
   ```bash
   cat > /etc/fail2ban/jail.d/tinyproxy.conf << 'EOF'
   [tinyproxy]
   enabled = true
   port = 8080
   filter = tinyproxy
   logpath = /var/log/tinyproxy/tinyproxy.log
   maxretry = 3
   bantime = 3600
   findtime = 600
   EOF
   ```
   Нажмите Enter

4. Создайте фильтр:
   ```bash
   cat > /etc/fail2ban/filter.d/tinyproxy.conf << 'EOF'
   [Definition]
   failregex = ^.*\[WARNING\].*Connection from <HOST> refused.*$
               ^.*\[ERROR\].*Connection from <HOST> failed.*$
   ignoreregex =
   EOF
   ```
   Нажмите Enter

5. Перезапустите защиту:
   ```bash
   systemctl restart fail2ban
   ```
   Нажмите Enter
   
   ```bash
   systemctl enable fail2ban
   ```
   Нажмите Enter

**Результат:** Теперь подозрительные IP будут автоматически блокироваться.

---

### ШАГ 6: Ограничить использование памяти прокси

**Что это:** Не давать прокси использовать больше 512 МБ памяти

**Как сделать:**
1. Вы все еще должны быть подключены к серверу
2. Откройте настройки службы прокси:
   ```bash
   systemctl edit tinyproxy
   ```
   Нажмите Enter
3. Откроется редактор. Вставьте эти строки:
   ```
   [Service]
   MemoryLimit=512M
   CPUQuota=50%
   TasksMax=50
   ```
4. Нажмите `Ctrl + O` (сохранить), затем Enter
5. Нажмите `Ctrl + X` (выйти)
6. Примените изменения:
   ```bash
   systemctl daemon-reload
   ```
   Нажмите Enter
   
   ```bash
   systemctl restart tinyproxy
   ```
   Нажмите Enter

**Результат:** Прокси не будет использовать больше 512 МБ памяти.

---

### ШАГ 7: Проверить, что все работает

**Что это:** Убедиться, что изменения не сломали ничего важного

**Как сделать:**
1. Вы все еще должны быть подключены к серверу
2. Проверьте количество подключений (должно быть меньше 20):
   ```bash
   ss -tn | grep :8080 | wc -l
   ```
   Нажмите Enter
   - Если число меньше 20 - отлично!
   - Если больше - подождите 5 минут и проверьте снова

3. Проверьте использование памяти:
   ```bash
   free -h
   ```
   Нажмите Enter
   - Посмотрите на строку "Mem:" - если "used" меньше 80% - отлично!

4. Проверьте статус защиты:
   ```bash
   fail2ban-client status tinyproxy
   ```
   Нажмите Enter
   - Если видите "Status: Active" - отлично!

5. Выйдите с сервера:
   ```bash
   exit
   ```
   Нажмите Enter

---

## ✅ ГОТОВО!

После выполнения всех шагов:
- ✅ Только вы можете использовать прокси
- ✅ Прокси не будет перегружен (максимум 20 подключений)
- ✅ Подозрительные IP автоматически блокируются
- ✅ Прокси не будет использовать слишком много памяти
- ✅ Сервер будет работать быстрее

---

## ⚠️ ВАЖНО

1. **Если ваш IP меняется:** Если ваш интернет-провайдер меняет ваш IP адрес, вам нужно будет повторить ШАГ 3 с новым IP.

2. **Если что-то не работает:** Если после изменений что-то перестало работать, можно откатить изменения:
   ```bash
   ufw delete allow from ВАШ_IP to any port 8080 proto tcp
   ufw delete deny 8080/tcp
   ufw reload
   ```

3. **Резервная копия:** Перед изменениями создайте копию настроек (на всякий случай):
   ```bash
   cp /etc/tinyproxy/tinyproxy.conf /etc/tinyproxy/tinyproxy.conf.backup
   ```

---

## 📞 Если нужна помощь

Если что-то не получается:
1. Скопируйте текст ошибки
2. Сохраните его
3. Обратитесь за помощью, показав этот текст

---

**Время выполнения:** 30-60 минут  
**Сложность:** Простая (нужно только копировать команды)  
**Результат:** Защищенный и быстрый сервер
